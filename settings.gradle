pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url 'https://jitpack.io' } // ✅ Add this line
    }
}
rootProject.name = "server_status"
include ':app'
